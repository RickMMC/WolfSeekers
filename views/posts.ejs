<!DOCTYPE html>
<html lang="en">
	<head>
        <%- include('components/head') %>
		<title>Posts</title>
	</head>

	<body>
        <%- include('components/navbar') %>
		<header id="" class=""><style> header {height: 100px; background-color: white;} </style></header>
    	<div class="container">
			<ol class="breadcrumb">
				<li><a href="/">Home</a></li>
				<li class="active">Post</li>
			</ol>
			<article class="col-xs-12 maincontent">  
				<header class="page-header">
					<h1 class="page-title">Ver todos los post publicados</h1>
				</header>

			<div class="recuadro">
			<ul id="post-list">
				<% 
					// MAP: Recorre un arreglo y pasa por cada uno de los elementos
					// == for (let i = 0; i < posts.length; i++) 
					posts.map((post) => {
				%>
				<li>
					<div class="top-margin">
						<h2><%= post.title %></h2>
						<p><%= post.company %></p>
						<p><%= post.email %></p>
						<p><%= post.content %></p>
					</div>
				</li>
				<% }); %>
			</ul>
			</div>
		</div>
<style>
			/* Estilo para el recuadro */
			.recuadro {
			border: 1px solid #ccc;
			padding: 10px;
			border-radius: 5px;
			width: 80%; /* Puedes ajustar el ancho según tus necesidades */
			margin: 0 auto; /* Centrar el recuadro horizontalmente */
			}

			/* Estilo para cada <li> dentro del <ul> */
			#post-list li {
			list-style-type: none; /* Elimina los puntos por defecto de las listas */
			margin-bottom: 20px; /* Espacio entre cada elemento de la lista */
			}

			/* Estilos para el contenido dentro de cada <li> */
			#post-list li div.top-margin {
			padding: 10px;
			border: 1px solid #ddd;
			border-radius: 5px;
			background-color: #f9f9f9;
			}
		</style>

		<%- include('components/pie') %>
	</body>
</html>
